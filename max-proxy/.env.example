# Публичный адрес основного сервера — сюда форвардятся все запросы.
# Обязательно с https://, без слэша на конце.
UPSTREAM_URL=https://your-main-server.example.com

# Порт, который слушает прокси (bothost подставит свой автоматически — обычно 3000).
PORT=3000
